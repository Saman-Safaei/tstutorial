function exam(number1: number, number2: number): number {
  return number1 + number2;
}

function exam2(num1: number, num2: number): string {
  return num1.toString() + num2.toString();
}

function exam3(text: string): void {
  console.log(text);
}
